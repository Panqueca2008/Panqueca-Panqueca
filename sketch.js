function setup() {
  createCanvas(800, 600);
  background(0);
}

function draw() {
  stroke("rgb(194,7,194)");
  fill("rgb(14,14,229)");

 if (mouseIsPressed) {
   rect(mouseX,mouseY,20,35);}}
